dependencies {
    api platform(libs.spring.cloud.bom)
    api platform(libs.testcontainers.playtika.bom)

    runtimeOnly 'org.springframework.cloud:spring-cloud-starter-bootstrap'

    runtimeOnly(libs.testcontainers.junit4.mock)
}